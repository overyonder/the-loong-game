# Emit C for the judge's wasm32 clang instead of building a native program.
compileOnly
nimcache = "gen"
cpu = wasm32
os = any
mm = arc
threads = off
exceptions = goto
panics = on
define = useMalloc
define = danger
define = noSignalHandler
hints = off
